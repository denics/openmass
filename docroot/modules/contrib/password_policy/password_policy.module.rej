--- password_policy.module
+++ password_policy.module
@@ -227,7 +227,7 @@
  */
 function password_policy_user_presave(Drupal\Core\Entity\EntityInterface $entity) {
   
-  if (!$entity->id()) {
+  if (!$entity->id() || empty($entity->field_last_password_reset->value)) {
     $date = date('Y-m-d\TH:i:s');
     $entity->set('field_last_password_reset', $date);
     $entity->set('field_password_expiration', '0');
@@ -266,6 +266,13 @@
   foreach($policies as $policy){
     //check each user's roles
     foreach($users as $user){
+      // Add password last update date if empty.
+      if (empty($user->field_last_password_reset->value)) {
+        $user->field_last_password_reset->value = gmdate(DATETIME_DATETIME_STORAGE_FORMAT, REQUEST_TIME);
+        $user->field_password_expiration->value = 0;
+        $user->save();
+      }
+
       //calculate expiration date
       $user_last_reset = $user->get('field_last_password_reset')->get(0)->getValue();
       $time = strtotime($user_last_reset['value']);
